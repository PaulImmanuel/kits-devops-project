# File: kubernetes/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: my-web-app-service
spec:
  type: LoadBalancer # Exposes the app with a public IP
  ports:
  - port: 80
    targetPort: 8080
  selector:    # <-- Corrected: Indented to be part of 'spec'
    app: my-web-app